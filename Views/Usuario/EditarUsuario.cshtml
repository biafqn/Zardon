@using Inova.Enums
@model UsuarioModel
@{
    ViewData["Title"] = "Editar Usuário";
    Layout = "~/Views/Shared/LayoutCriar.cshtml";
}
    <link rel="stylesheet" href="~/css/criar.css">


    <div class="container">
        <h2>Edição de Usuários 📋</h2>
        <form asp-controller="Usuario" asp-action="AlterarUsuario" method="post">
            <input type="hidden" asp-for="Id" />
            <div class="mb-3">
                <label for="itemName" class="form-label">Nome do Usuário</label>
                <input type="text" asp-for="Nome" class="form-control" id="itemName" placeholder="Ex: João da Silva" required>
                @Html.ValidationMessageFor(x => x.Nome, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                <label for="finderName" class="form-label">Login</label>
                <input type="text" asp-for="Login" class="form-control" id="finderName" placeholder="Digite o login do usuário" required>
                @Html.ValidationMessageFor(x => x.Login, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                <label for="contactNumber" class="form-label">Número do Usuário</label>
                <input type="tel" asp-for="Celular" class="form-control" id="contactNumber" placeholder="(XX) XXXXX-XXXX" required>
                @Html.ValidationMessageFor(x => x.Celular, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                <label for="contactEmail" class="form-label">Email para Contato</label>
                <input type="email" asp-for="Email" class="form-control" id="contactEmail" placeholder="seuemail@exemplo.com" required>
                @Html.ValidationMessageFor(x => x.Email, "", new { @class = "text-danger" })
            </div>

            <div class="mb-3">
                <label for="userProfile" class="form-label">Perfil</label>
                <select asp-for="Perfil" name="Perfil" id="userProfile" class="form-select" required>
                    <option selected disabled value="0">Selecione o Perfil do Usuário</option>
                    <option value="@PerfilEnum.Admin">Admin</option>
                    <option value="@PerfilEnum.Padrao">Padrão</option>
                </select>
                @Html.ValidationMessageFor(x => x.Perfil, "", new { @class = "text-danger" })
            </div>

            <div class="d-grid mt-4">
                <button type="submit" class="btn btn-primary">Editar</button> 
            </div>
            <div class="d-grid mt-4">
                <a asp-controller="Usuario" asp-action="TabelaUsuarios" class="btn btn-secondary">Voltar</a>
            </div>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>